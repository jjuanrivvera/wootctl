# .goreleaser.yaml — wootctl release pipeline (cliwright GOAL.md §5).
# Replace wootctl / jjuanrivvera / wootctl. Run `goreleaser check` in CI (not only at tag).
version: 2

before:
  hooks: [go mod tidy, ./scripts/completions.sh]

builds:
  - id: wootctl
    main: ./cmd/wootctl
    binary: wootctl
    env: [CGO_ENABLED=0]
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - { goos: windows, goarch: arm64 }
    ldflags:
      - -s -w
      - -X {{ .ModulePath }}/internal/version.Version={{ .Version }}
      - -X {{ .ModulePath }}/internal/version.Commit={{ .Commit }}
      - -X {{ .ModulePath }}/internal/version.Date={{ .Date }}

archives:
  - formats: [tar.gz]
    format_overrides:
      - { goos: windows, formats: [zip] }
    files: [README.md, LICENSE, completions/*]

checksum: { name_template: checksums.txt }

# Docker: a tiny distroless image built from the prebuilt static amd64 binary.
# CI snapshot runs pass --skip=docker (no registry auth on PRs); the real image is built and
# pushed to GHCR at tag time (the release workflow logs in to ghcr.io first).
dockers:
  - id: wootctl
    ids: [wootctl]
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile.goreleaser
    image_templates:
      - "ghcr.io/jjuanrivvera/wootctl:{{ .Version }}"
      - "ghcr.io/jjuanrivvera/wootctl:v{{ .Major }}"
      - "ghcr.io/jjuanrivvera/wootctl:latest"
    build_flag_templates:
      - "--label=org.opencontainers.image.title=wootctl"
      - "--label=org.opencontainers.image.description=A fast, scriptable CLI for the full Chatwoot API"
      - "--label=org.opencontainers.image.source=https://github.com/jjuanrivvera/wootctl"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.licenses=MIT"

# An explicit release block — without it, `goreleaser check` errors on a fresh repo
# ("scm releases: no remote configured to list refs from").
release:
  github: { owner: jjuanrivvera, name: wootctl }
  prerelease: auto

# Homebrew: use homebrew_casks (NOT the deprecated brews:). The cask is wootctl-cli,
# but the binary inside the archive is wootctl — so `binaries:` (plural list) is required,
# and an unsigned arm64 binary is SIGKILL'd by Gatekeeper without the xattr postflight.
homebrew_casks:
  - name: wootctl-cli
    binaries: [wootctl]
    # token required to push the cask cross-repo: the default GITHUB_TOKEN is scoped to this
    # repo only and cannot write to the tap.
    repository: { owner: jjuanrivvera, name: homebrew-wootctl, token: "{{ .Env.HOMEBREW_TAP_TOKEN }}" }
    homepage: https://github.com/jjuanrivvera/wootctl
    hooks:
      post:
        install: |
          system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/wootctl"]

scoops:
  - repository: { owner: jjuanrivvera, name: scoop-wootctl, token: "{{ .Env.HOMEBREW_TAP_TOKEN }}" }   # bucket repo must already exist; cross-repo token

nfpms:
  - formats: [deb, rpm, apk]
    bindir: /usr/bin
    maintainer: jjuanrivvera
    contents:
      - { src: completions/wootctl.bash, dst: /usr/share/bash-completion/completions/wootctl }
      - { src: completions/wootctl.zsh,  dst: /usr/share/zsh/site-functions/_wootctl }
      - { src: completions/wootctl.fish, dst: /usr/share/fish/vendor_completions.d/wootctl.fish }

sboms:
  - artifacts: archive          # syft SBOMs

signs:                          # cosign keyless (needs CI OIDC: permissions id-token: write)
  - cmd: cosign
    artifacts: checksum
    output: true
    args: ["sign-blob", "--yes", "--output-signature=${signature}", "--output-certificate=${certificate}", "${artifact}"]

changelog:
  groups:
    - { title: Features, regexp: '^.*?feat(\(.+\))??!?:.+$', order: 0 }
    - { title: Fixes, regexp: '^.*?fix(\(.+\))??!?:.+$', order: 1 }
    - { title: Performance, regexp: '^.*?perf(\(.+\))??!?:.+$', order: 2 }
  filters:
    exclude: ['^docs:', '^test:', '^chore:', '^ci:', merge conflict, Merge pull request, Merge branch]
